# Base URL of the deployed server. In local dev the client falls back to
# http://localhost:4001 when this is unset.
#
# Production example:
#   VITE_API_BASE_URL=https://softwaresellr-api.vercel.app
VITE_API_BASE_URL=
